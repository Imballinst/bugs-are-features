// Copyright (c) Tetrate, Inc 2020 All Rights Reserved.

/**
 * Find the index of the first repeating character. If nothing is found, return -1.
 * @param wordOrSentence The string passed as word or sentence.
 * @param character The string that we want to check.
 * @param threshold The number of characters needed for the function to return immediately. Defaults to 2.
 */
export function findIndexOfRepeatingCharacter(
  wordOrSentence: string,
  character: string,
  threshold: number = 2
): number {
  throw new Error('Not implemented yet.');
}

/**
 * Remove duplicate elements from an array of numbers.
 * @param array Array of numbers.
 */
export function removeDuplicatesFromArray(array: number[]): number[] {
  throw new Error('Not implemented yet.');
}

/**
 * Convert an object to a map.
 * @param object An object with key-value strings.
 */
export function convertObjectToMap(object: {
  [index: string]: string;
}): Map<string, string> {
  throw new Error('Not implemented yet.');
}
